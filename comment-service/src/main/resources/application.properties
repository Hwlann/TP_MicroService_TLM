spring.application.name=comment-service
server.port = 8484

database = h2
spring.datasource.data=classpath*:db/${database}/

management.endpoint.hystrix.stream.enabled=true
management.endpoints.web.exposure.include=hystrix.stream